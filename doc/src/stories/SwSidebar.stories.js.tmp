import '../assets/styles/tailwind.css';

import SwSidebar from '../snowind/sidebar/SwSidebar.vue';
import SwSidebarModel from '../snowind/sidebar/model';

export default {
  title: 'Components/Sidebar',
  component: SwSidebar,
};

const model = new SwSidebarModel();

const Template = (args) => ({
  // Components used in your story `template` are defined in the `components` object
  components: { SwSidebar },
  // The story's `args` need to be mapped into the template through the `setup()` method
  setup() {
    // Story args can be spread into the returned object
    return { ...args };
  },
  // Then, the spread values can be accessed directly in the template
  template: `<sw-sidebar :model="model" class="bg-secondary">
  <div>Item 1</div>
  <div>Item 2</div>
  <div>Item 3</div>
  <div>Item 4</div>
  </sw-sidebar>
  `,
});

export const Expanded = Template.bind({});
Expanded.args = {
  model: new SwSidebarModel({ isOpened: true }),
};

export const Collapsed = Template.bind({});
Collapsed.args = {
  model: model,
};
